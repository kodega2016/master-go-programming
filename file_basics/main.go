package main

func main() {
	// var newFile *os.File
	// var err error

	// fmt.Printf("%T\n", newFile)

	// newFile, err = os.Create("server.txt")

	// if err != nil {
	// 	fmt.Println("error:", err)
	// 	os.Exit(1)
	// }

	// fmt.Println("file created...", newFile.Name())

	// _ = os.Truncate("server.txt", 0)

	// newFile.Close()

	/*
		// check file information
		f, _ := os.Stat("server.txt")
		fmt.Println("file info:", f.Name(), f.Size())
		fmt.Println("last modified:", f.ModTime())
		fmt.Println("is directory:", f.IsDir())
		fmt.Println("permissons:", f.Mode())

	*/

	/*
		// rename file name
		os.Rename("server.txt", "app.txt")
	*/

	// removing file
	// os.Remove("app.txt")

}
